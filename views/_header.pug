// Header
header.header
    nav.nav.nav--tours
        a.nav__el(href='/') همۀ تور ها
        form.nav__search
            button.nav__search-btn
                svg
                    use(xlink:href='/img/icons.svg#icon-search')
            input.nav__search-input(type='text' placeholder='جستجو در تور ها')
    .header__logo
        img(src='/img/logo-white.png' alt='Natours logo')
    nav.nav.nav--user
        a.nav__el(href='#') رزروهای من
        if user
            a.nav__el.nav__el--logout خروج
                a.nav__el(href="/me")
                    img.nav__user-img(src=`/img/users/${user.photo}` alt=`${user.name}`)
                    span= user.name.split(' ')[0]
        else
            a.nav__el(href='/login') ورود
            a.nav__el.nav__el--cta(href='/signup') ثبت نام